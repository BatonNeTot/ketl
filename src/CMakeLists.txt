cmake_minimum_required (VERSION 3.10)

file(GLOB_RECURSE PROJECT_SOURCES CONFIGURE_DEPENDS *.c *.h *.s)
file(GLOB_RECURSE PROJECT_SOURCES_ASM CONFIGURE_DEPENDS *.s)
set(PROJECT_SOURCES "${PROJECT_SOURCES}" PARENT_SCOPE)
set(PROJECT_SOURCES_ASM "${PROJECT_SOURCES_ASM}" PARENT_SCOPE)
