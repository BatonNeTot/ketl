import token

export class Lexer {
    var tokens : token.Token[];
    //var lines : str[]; // TODO string = array[char]
    //var filename : astr; TODO atomic string
    //var source : str; TODO string = array[char]
    var length : u32;
    var offset : u32;
    var token_iterator : token.Token.Iterator;
}
